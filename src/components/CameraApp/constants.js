import {Camera} from "expo-camera";

export const CAMERA_TYPES = [Camera.Constants.Type.back,Camera.Constants.Type.front];
export const CAMERA_FLASH_MODES = [Camera.Constants.FlashMode.off, Camera.Constants.FlashMode.on];